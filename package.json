{
  "name": "tasks-nodes",
  "version": "1.0.0",
  "description": "Task for Node",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "dev": "tsc && node --watch dist/server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.11.20",
    "typescript": "^5.3.3"
  }
}
